from setuptools import setup

setup(
   name='chess_game',
   version='1.0',
   description='Server-client chess game',
   author='G.Kmita, M.Moskal, S.Kopulski',
   install_requires=['chess', 'numpy', 'pandas'],
)
